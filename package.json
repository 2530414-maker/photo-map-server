{
  "name": "photo-map-server",
  "version": "1.0.0",
  "description": "Upload markers without auth; admin approves/rejects with Firebase token; awards points.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "firebase-admin": "^12.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
